server.port=2200
spring.jpa.properties.hibernate.temp.use_jdbc_metadata_defaults=false

spring.datasource.url=jdbc:postgresql://localhost:2201/miro
spring.datasource.username=miro
spring.datasource.password=miro
spring.datasource.driver-class-name=org.postgresql.Driver
spring.jpa.database=postgresql
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQL9Dialect
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQL9Dialect
spring.jpa.show-sql=false

spring.jpa.properties.hibernate.jdbc.lob.non_contextual_creation=true
hibernate.jdbc.lob.non_contextual_creation=true

all-data-table=schedule.all_data

spring.jpa.hibernatedialect=org.hibernate.dialect.PostgreSQL9Dialect
spring.jpa.hibernate.show_sql=false
properties.hibernate.temp.use_jdbc_metadata_defaults=false

spring.jpa.open-in-view=true

spring.flyway.enabled=false

spring.liquibase.enabled=true
spring.liquibase.change-log=classpath:/liquibase/master.xml
spring.liquibase.contexts=dev

spring.main.allow-bean-definition-overriding=true

jwt.signing.key.secret=mySecret
jwt.get.token.uri=/api/login
jwt.refresh.token.uri=/api/refresh
jwt.http.request.header=Authorization
jwt.token.expiration.in.seconds=604800

